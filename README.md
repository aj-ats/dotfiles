# paths to move  
1. `mv .tmux.conf ~ && mv .partial.bashrc ~/.bashrc` assuming your on a new system otherwise `cat .partial.bashrc >> ~/.bashrc` 
3. `mv dotfiles/* ~/.config/nvim`
4. not restructring git repo rn and no one will use this besdies me so dosnt rly matter yktv 
